/**
 * @module Configuration
 * @description 应用全局配置
 */
import config from 'bf-main-configs';

export default {
  // 服务接口域名
  API: config.API,
  // 云服务器图片服务Url
  CDN: config.CDN,
  // 当前域名
  HOST: config.HOST,
};
